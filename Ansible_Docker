---
- name: Build and Run Docker Container
  hosts: localhost
  tasks:
    - name: Build Docker image
      docker_image:
        name: myapp
        tag: latest
        build:
          path: "/var/lib/jenkins/workspace/Ansible_Docker
          dockerfile: Dockerfile.
 
    - name: Run Docker container
      docker_container:
        name: myapp_container
        image: myapp:latest
        state: started
        ports:
          - "8083:8080"
